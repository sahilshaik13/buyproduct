[build]
  command = "pip install -r requirements_numpy.txt && pip install -r requirements_other.txt && python build.py"
  publish = "build"
  environment = { PYTHON_VERSION = "3.11.4" }  # For compatibility, as discussed
